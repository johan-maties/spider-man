# PostgreSQL connection string for local development
# Replace username, password, host, port, and database name.
DATABASE_URL=postgres://username:password@localhost:5432/spiderman

# For Render, set this in the service environment variables instead of using .env
# NODE_ENV=production
